// Slot owned by the agent workstream (docs/WORKSTREAMS.md, C). The app shell renders this in its
// dockable right-hand panel; replace the body, keep the export name and props.
export interface AgentPanelProps {
  onClose: () => void;
}

export function AgentPanel(_props: AgentPanelProps) {
  return null;
}
