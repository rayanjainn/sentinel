// Slot owned by the agent workstream (docs/WORKSTREAMS.md, C). Rendered inside the shell's Settings
// view under "AI agent"; replace the body, keep the export name.
export function AgentSettingsSection() {
  return null;
}
